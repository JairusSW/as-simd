name: Run Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: false

      - name: Install
        run: npm install

      - name: Unit tests
        run: npm test

      - name: Transform tests
        run: npm run test:transform

      - name: Latest Wago and Wide integration
        run: npm run test:wago-wide
