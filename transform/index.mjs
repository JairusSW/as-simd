import { Transform } from "assemblyscript/dist/transform.js";

export default class AsSimdTransform extends Transform {
  // Manual-only mode: keep transform as a no-op entrypoint for compatibility.
}
